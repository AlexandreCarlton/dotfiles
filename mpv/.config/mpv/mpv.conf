[default]

# Cache to avoid buffering pauses
cache=yes

# Disabled for performance reasons - apparently not an option?
# deband=no

# Enable hardware decoding if supported
hwdec=auto

# FFmpg doesn't currently support .mpd demuxing, hence the below setting.
ytdl-format=[protocol!=http_dash_segments]
