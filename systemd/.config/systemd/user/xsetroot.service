[Unit]
Description=xsetroot - root window parameter setting utility for X
Documentation=man:xsetroot(1)
After=x11.target

[Service]
Type=oneshot
ExecStart=/usr/bin/xsetroot -cursor_name left_ptr

[Install]
# This is X-specific; we want it only if xorg is used.
WantedBy=x11.target
