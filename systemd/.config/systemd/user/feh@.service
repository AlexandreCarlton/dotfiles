[Unit]
Description=feh (wallpaper)
Documentation=man:feh(1)
After=x11.target

[Service]
Type=oneshot
ExecStart=/usr/bin/feh --verbose --no-fehbg --bg-fill %h/.wallpaper/%I
RemainAfterExit=yes

[Install]
# This is only needed by xorg; weston already has settings for wallpaper.
WantedBy=x11.target
