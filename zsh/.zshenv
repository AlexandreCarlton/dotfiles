#!/bin/zsh

# This file (.zshenv) is always expected in $HOME.
# It can define ZDOTDIR, which is where the rest of the files may go.

[[ -s "${HOME}/.config/sh/env" ]] && source "${HOME}/.config/sh/env"
